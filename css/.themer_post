#!/usr/bin/env bash

declare -A TMR
eval "$(themer --list varsh)"

cp .themer/${TMR[name]}/hyde.css   hyde.css
cp .themer/${TMR[name]}/poole.css  poole.css
cp .themer/${TMR[name]}/themer.css themer.css

cd ..
convert -fill "${TMR[fg0]}" -opaque black -fuzz 50% \
	about_black.png about.png
