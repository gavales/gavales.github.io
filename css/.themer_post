#!/usr/bin/env bash

declare -A TMR
eval "$(themer --list varsh)"

cp .themer/${TMR[name]}/page.css page.css
cp .themer/${TMR[name]}/post.css post.css

cd ..
convert -fill "${TMR[fg0]}" -opaque black -fuzz 50% \
	about_black.png about.png
